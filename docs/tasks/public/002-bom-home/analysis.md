# BomHomeContainer 画面設計分析

## 概要

BOM（部品表）ホーム画面のメインコンテナコンポーネント。製品一覧から製品を選択し、BOMツリー構造を表示、ノードの詳細情報を確認できる画面。

**ファイルパス**: `frontend/src/page-components/bom/home/ui/BomHomeContainer.tsx`

---

## 画面設計 ASCII

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              BomHomeContainer (flex min-h-0 flex-1)                     │
├────────────────────┬────────────────────────────────────────────────────────────────────┤
│                    │                                                                    │
│  ProductSearchPanel│                   メインコンテンツ (flex-1 px-6 py-4)              │
│  (w-64, border-r)  │ ┌────────────────────────────────────────────────────────────────┐ │
│                    │ │ ヘッダー (mb-4 flex justify-between)                           │ │
│ ┌────────────────┐ │ │ ┌──────────────────────────────────┐  ┌──────────────────────┐ │ │
│ │ 🔍 製品番号... │ │ │ │ h2: 製品名 / "BOM構成"           │  │ [Canvas表示] ボタン  │ │ │
│ └────────────────┘ │ │ └──────────────────────────────────┘  └──────────────────────┘ │ │
│                    │ └────────────────────────────────────────────────────────────────┘ │
│ ┌────────────────┐ │                                                                    │
│ │  製品リスト    │ │ ┌──────────────────────────────────────┬─────────────────────────┐ │
│ │  (ScrollArea)  │ │ │                                      │                         │ │
│ │                │ │ │  BomTreeBlock (flex-1)               │  BomDetailPanel (w-96)  │ │
│ │ ┌────────────┐ │ │ │  ┌──────────────────────────────┐    │  (条件付き表示)         │ │
│ │ │ ARM-1000   │ │ │ │  │ BomTreePanel                 │    │  ┌───────────────────┐  │ │
│ │ │ ロボット.. │ │ │ │  │ ┌──────────────────────────┐ │    │  │ ヘッダー          │  │ │
│ │ └────────────┘ │ │ │  │ │ 📁 Assy-001             │ │    │  │ 製品名 [X]        │  │ │
│ │ ┌────────────┐ │ │ │  │ │   📁 SubAssy-001        │ │    │  ├───────────────────┤  │ │
│ │ │ ...        │ │ │ │  │ │     📁 Module-001       │ │    │  │ 基本情報          │  │ │
│ │ └────────────┘ │ │ │  │ │       📄 Part-001       │ │    │  │ - ID              │  │ │
│ │                │ │ │  │ │       📄 Part-002       │ │    │  │ - 数量            │  │ │
│ │ (選択状態で   │ │ │  │ │   📁 SubAssy-002        │ │    │  │ - 作成日          │  │ │
│ │  bg-accent)   │ │ │  │ │ 📁 Assy-002             │ │    │  │ - 更新日          │  │ │
│ │                │ │ │  │ └──────────────────────────┘ │    │  ├───────────────────┤  │ │
│ │ 検索結果なし  │ │ │  │                              │    │  │ 属性              │  │ │
│ │ → NoData表示  │ │ │  │ 製品未選択時:               │    │  │ (customItems)     │  │ │
│ │                │ │ │  │ → NoData表示               │    │  ├───────────────────┤  │ │
│ │                │ │ │  │ "左サイドバーから製品を...  │    │  │ ドキュメント      │  │ │
│ │                │ │ │  └──────────────────────────────┘    │  ├───────────────────┤  │ │
│ │                │ │ │                                      │  │ 図面              │  │ │
│ └────────────────┘ │ │                                      │  ├───────────────────┤  │ │
│                    │ │                                      │  │ [詳細ページを開く]│  │ │
│                    │ │                                      │  └───────────────────┘  │ │
│                    │ └──────────────────────────────────────┴─────────────────────────┘ │
│                    │                                                                    │
└────────────────────┴────────────────────────────────────────────────────────────────────┘
```

---

## 状態管理

| State | 型 | 初期値 | 用途 |
|-------|-----|--------|------|
| `selectedProductId` | `string \| null` | `null` | 選択された製品ID |
| `selectedNodeId` | `string \| null` | `null` | 選択されたBOMノードID |

### 派生データ

| 変数 | 取得方法 | 説明 |
|------|----------|------|
| `selectedProduct` | `dummyProducts.find()` | 選択製品情報 |
| `bomTree` | `getBomByProductId()` | 製品のBOMツリー（BomNode[]） |
| `treeNodes` | `bomNodesToTreeNodes()` | ツリー表示用データ（TreeNode[]） |
| `selectedNodeDetail` | `getNodeDetailById()` | 選択ノードの詳細情報 |

---

## イベントハンドラ

| ハンドラ | 引数 | 動作 |
|----------|------|------|
| `handleSelectProduct` | `productId: string` | 製品選択、ノード選択をリセット |
| `handleSelectNode` | `node: TreeNode` | BOMノード選択 |
| `handleCloseDetail` | なし | 詳細パネルを閉じる |

---

## 影響範囲ツリー

```
BomHomeContainer
├── State
│   ├── selectedProductId (string | null)
│   └── selectedNodeId (string | null)
│
├── Data Sources (dummy-data/bom/products.ts)
│   ├── dummyProducts: Product[]
│   ├── getBomByProductId(id): BomNode[]
│   ├── bomNodesToTreeNodes(nodes): TreeNode[]
│   └── getNodeDetailById(id): BomNodeDetail
│
├── UI Components
│   │
│   ├── ProductSearchPanel (ui-block/product-search-panel)
│   │   ├── Props
│   │   │   ├── products: Product[]
│   │   │   ├── selectedProductId: string | null
│   │   │   └── onSelectProduct: (id: string) => void
│   │   ├── Internal State
│   │   │   └── searchQuery: string
│   │   └── Dependencies
│   │       ├── @/shared/ui/shadcn/ui/input
│   │       ├── @/shared/ui/shadcn/ui/scroll-area
│   │       ├── @/shared/ui/components/empty-design/ui/NoData
│   │       └── lucide-react (Search icon)
│   │
│   ├── メインコンテンツヘッダー
│   │   ├── h2: selectedProduct?.productName ?? 'BOM構成'
│   │   └── Button (条件: selectedProductId存在時)
│   │       └── Link → /bom/{productId}/canvas
│   │
│   ├── BomTreeBlock (ui-block/bom-tree)
│   │   ├── Props
│   │   │   ├── treeNodes: TreeNode[]
│   │   │   ├── selectedNodeId: string | null
│   │   │   └── onSelectNode: (node: TreeNode) => void
│   │   └── Dependencies
│   │       └── BomTreePanel (widgets/bom/bom-tree-panel)
│   │           ├── Props
│   │           │   ├── treeNodes: TreeNode[]
│   │           │   ├── selectedNodeId: string | null
│   │           │   ├── onSelectNode?: (node: TreeNode) => void
│   │           │   ├── allowedTypes: GalleryItemType[] (固定: ['directory'])
│   │           │   ├── defaultOpenLevel: number (default: 2)
│   │           │   └── emptyMessage: string
│   │           └── Dependencies
│   │               ├── TreeNodeItem (tree-components)
│   │               ├── @/shared/ui/shadcn/ui/card
│   │               └── @/shared/ui/components/empty-design/ui/NoData
│   │
│   └── BomDetailPanel (ui-block/detail-panel) [条件付き表示]
│       ├── 表示条件: selectedNodeDetail が存在する場合
│       ├── Props
│       │   ├── detail: BomNodeDetail
│       │   └── onClose: () => void
│       └── Dependencies
│           ├── next/navigation (useRouter)
│           ├── @/shared/ui/shadcn/ui/button
│           ├── @/shared/ui/shadcn/ui/badge
│           ├── @/shared/ui/shadcn/ui/card
│           ├── @/shared/ui/shadcn/ui/scroll-area
│           ├── @/shared/ui/shadcn/ui/separator
│           └── lucide-react (ExternalLink, FileText, Image, X)
│
└── Navigation Links
    ├── /bom/{productId}/canvas (Canvas表示ボタン)
    └── /bom/{nodeId}/basic-information (詳細ページを開くボタン)
```

---

## ファイル構成

```
frontend/src/page-components/bom/home/
├── ui/
│   └── BomHomeContainer.tsx          # メインコンテナ
└── ui-block/
    ├── product-search-panel/
    │   └── ui/
    │       └── ProductSearchPanel.tsx # 製品検索サイドバー
    ├── bom-tree/
    │   └── ui/
    │       └── BomTreeBlock.tsx       # BOMツリーブロック
    └── detail-panel/
        └── ui/
            └── BomDetailPanel.tsx     # 詳細パネル
```

---

## 型定義

### Product
```typescript
interface Product {
  id: string;
  productNumber: string;
  productName: string;
  category: string;
}
```

### TreeNode
```typescript
interface TreeNode {
  id: string;
  type: GalleryItemType; // 'directory' | 'document' | 'drawing'
  name: string;
  nodeType?: NodeType;   // '製品' | 'Assy' | 'SubAssy' | 'SubSubAssy' | 'Module' | 'Part'
  children?: TreeNode[];
}
```

### BomNodeDetail
```typescript
interface BomNodeDetail {
  id: string;
  name: string;
  nodeType: NodeType;
  quantity: number;
  customItems: Record<string, string | number | boolean>;
  documents: { id: string; typeName: string; name: string }[];
  drawings: { id: string; name: string; previewImageUrl: string }[];
  createdAt: string;
  updatedAt: string;
}
```

---

## データフロー図

```
┌─────────────────┐     onSelectProduct     ┌─────────────────────────────────────┐
│ ProductSearch   │ ───────────────────────▶ │ BomHomeContainer                    │
│ Panel           │                         │                                     │
└─────────────────┘                         │  selectedProductId                  │
                                            │       │                             │
                                            │       ▼                             │
┌─────────────────┐                         │  getBomByProductId()                │
│ dummyProducts   │ ───────────────────────▶│       │                             │
└─────────────────┘                         │       ▼                             │
                                            │  bomNodesToTreeNodes()              │
                                            │       │                             │
                                            │       ▼                             │
                                            │  treeNodes ──────┐                  │
                                            │                  │                  │
                                            └──────────────────│──────────────────┘
                                                               │
                                                               ▼
                                            ┌─────────────────────────────────────┐
                                            │ BomTreeBlock                        │
                                            │      │                              │
                                            │      │ onSelectNode                 │
                                            │      ▼                              │
                                            │ selectedNodeId                      │
                                            │      │                              │
                                            │      ▼                              │
                                            │ getNodeDetailById()                 │
                                            │      │                              │
                                            │      ▼                              │
                                            │ BomDetailPanel (if detail exists)   │
                                            └─────────────────────────────────────┘
```

---

## 補足

- **Canvas表示ボタン**: 製品選択時のみ表示、`/bom/{productId}/canvas`へ遷移
- **詳細パネル**: ノード選択時のみ右側に表示（w-96固定幅）
- **BomTreeBlock**: `allowedTypes: ['directory']`により、ディレクトリのみ表示
- **NoData表示**: 製品未選択時、検索結果なし時にそれぞれ表示
